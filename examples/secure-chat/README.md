Secure Chat Example (skeleton)

This example demonstrates per-user ACLs and role-based permissions. It is a
placeholder scaffold created as part of Phase 2; a full implementation can be
expanded to include server/client binaries and dynamic ACL updates.

To run (once implemented):

  cargo run -p secure-chat -- server
  cargo run -p secure-chat -- client alice
