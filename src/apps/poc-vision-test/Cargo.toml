[package]
name = "poc-vision-test"
version = "0.3.0-dev"
edition = "2021"

[dependencies]
actix = "0.13"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
bincode = "2"
serde = { version = "1", features = ["derive"] }

# Internal dependencies - using workspace versions
zznet-room = { path = "../../net/zznet-room" }
zznet-auth = { path = "../../net/zznet-auth" }
zzintent-config = { path = "../../components/zzintent-config" }
zzping-auth = { path = "../../common/zzping-auth" }

[dev-dependencies]
