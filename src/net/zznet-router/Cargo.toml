[package]
name = "zznet-router"
version = { workspace = true }
edition = { workspace = true }

[dependencies]
# Async runtime
tokio = { workspace = true, features = ["sync", "macros"] }

# Logging
tracing = { workspace = true }

# Actor framework for RouterActor
actix = { workspace = true }

# Internal dependencies
zznet-api = { workspace = true }
zznet-room = { workspace = true }
# Async trait helper macro for channel abstraction
async-trait.workspace = true
# Phase 2 will add: actix, bytes, zznet-api


[lints]
# This makes the crate inherit all lints defined in [workspace.lints]
workspace = true
