[package]
name = "phase6_integration_tests"
version = "0.1.0"
edition = "2024"

[dependencies]
# Reuse the workspace-generated proto and test utils
zzping-proto = { path = "../old/common/zzping-proto" }
zzping-test-utils = { path = "../test-utils/zzping-test-utils" }

[dev-dependencies]
# We will use tokio + tonic for test helpers
tokio = { workspace = true }
tonic = { workspace = true }
rustls = { workspace = true }
rustls-pemfile = { workspace = true }
base64 = { workspace = true }
tokio-rustls = { workspace = true }
ntest = { workspace = true }

[package.metadata.cargo-leak-check]
# placeholder
